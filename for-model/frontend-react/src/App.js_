fileName: App.js
---

import './App.css';
import 
import { Grid, Card, CardActions, CardContent, Button, Stack, Typography } from '@mui/material';


function App() {
  return (
    <div>
      <header>상단</header>
      <div className="left-menu">
        {{#boundedContexts}}
          {{#aggregates}}
            <div>{{namePascalCase}}</div>
          {{/aggregates}}
        {{/boundedContexts}}
      </div>
      <section>메인</section>
    </div>
  );
}

export default App;
